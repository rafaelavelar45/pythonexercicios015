salário = float(input('Digite o salario do funcionario: R$'))
reajuste = salário * 0.15
reajuste2 = salário + reajuste
print('O salário desse funcionario com  reajuste de 15% vai ser R$ {:.2f}'.format(reajuste2))

